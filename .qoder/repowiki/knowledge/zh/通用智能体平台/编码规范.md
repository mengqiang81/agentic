- 响应式编程规范：所有 Controller 接口均返回 Mono/Flux，并在 Service 层通过 subscribeOn(Schedulers.boundedElastic()) 处理阻塞调用。
- 租户上下文传递：统一通过 X-Tenant-Id 和 X-User-Id Header 获取身份，并在底层拼合为 tenantId:userId 格式作为 Agent 的用户标识。
- 配置外部化：关键参数（如模型地址、沙箱镜像、工作区路径）均通过 application.yml 定义并使用 @Value 注入，支持环境变量覆盖。