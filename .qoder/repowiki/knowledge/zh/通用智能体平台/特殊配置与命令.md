- **环境变量**：需设置 `DEEPSEEK_API_KEY` 和 `REDIS_URI`（默认指向 db 1）。
- **启动命令**：使用 `mvn spring-boot:run` 或打包后运行 `java -jar target/agentic-0.0.1-SNAPSHOT.jar`。
- **依赖管理**：显式覆盖 Jedis 版本至 7.4.1 以兼容 `agentscope-extensions-redis`。